import { useState } from "react";

function Item() {
    const [data,setData]=useState([])
    return (  
        <React.Fragment>
            <div className={styles.nav}>
             
            </div>
        </React.Fragment>

    );
}

export default Item;